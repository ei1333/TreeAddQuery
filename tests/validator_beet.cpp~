#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

const int MIN_N = 2;
const int MAX_N = 100000;

int main() {
    registerValidation();
    int N = inf.readInt(MIN_N, MAX_N, "N");
    inf.readSpace();
    int K = inf.readInt( MIN_N, N, "K");
    inf.readEoln();
    set<int>st;
    for(int i=0;i<N;i++){
    	if( i )inf.readSpace();
  		int x = inf.readInt(1, N, "x");
  		st.insert(x);
	}
	assert( (int)st.size() == N );
	inf.readEoln();
	inf.readEof();
	return 0;
}
